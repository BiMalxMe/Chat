{
  "name": "sapienschat",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "backend:dev": "npm install --prefix backend && npm run dev --prefix backend",
    "frontend:dev": "npm install --prefix frontend && npm run dev --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
